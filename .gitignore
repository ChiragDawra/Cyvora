# macOS
.DS_Store

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Environment / secrets
.env
.env.*

# Local point-in-time feed downloads (not source, don't belong in git history)
/known_exploited_vulnerabilities.json

# Node
node_modules/
.next/
out/
dist/
build/

# Python
__pycache__/
*.pyc
.venv/
venv/
ingestion/build/
backend/build/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
# NOTE: .terraform.lock.hcl is deliberately NOT ignored - Terraform recommends
# committing it to pin provider versions across machines.
